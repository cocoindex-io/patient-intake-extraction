[project]
name = "patient-intake-extraction"
version = "0.1.0"
description = "CocoIndex v1 example: extract structured data from patient intake forms with DSPy + a vision LLM."
requires-python = ">=3.11"
dependencies = [
    "cocoindex>=1.0.7",
    "dspy-ai>=3.0.4",
    "pillow>=10.0.0",
    "pydantic>=2.12.5",
    "pymupdf>=1.26.5",
    "python-dotenv>=1.0.1",
]

[tool.setuptools]
packages = []
